{
  "typescript": {
    "deferredLatest": "7.0.2",
    "reviewed": "2026-08-22",
    "reason": "tsc -b passes on 7.0.2, but typescript-eslint refuses to load against it (\"typescript-eslint does not support TS 7.0\", peer range >=4.8.4 <6.1.0, tracking issue typescript-eslint#10940), so npm run check cannot lint. TS 6.0.x is inside that peer range and is the realistic next step; revisit when typescript-eslint ships TS >=7.1 support."
  },
  "three": {
    "deferredLatest": "0.185.1",
    "reviewed": "2026-08-22",
    "reason": "Nothing blocks it on paper -- @pixiv/three-vrm peers on three >=0.137, @react-three/fiber on >=0.156, drei on >=0.159. three.js still changes rendering behaviour across minors, and the VRM, VRMA and lip-sync paths can only be judged by looking at the avatar on a real Windows desktop. This is verification debt, not a compatibility block: pair it with a desktop verification round."
  },
  "@types/three": {
    "deferredLatest": "0.185.4",
    "reviewed": "2026-08-22",
    "reason": "Types must describe the three.js actually installed. Moves together with three, not before it."
  },
  "@types/node": {
    "deferredLatest": "26.4.0",
    "reviewed": "2026-08-29",
    "reason": "Node type definitions track the runtime the project builds on, not the newest published. CI runs Node 24 and package.json requires >=24; typing against Node 26 would surface APIs the build environment does not have. Re-confirmed 2026-08-29 against 26.4.0: the reasoning is unchanged, only the version the deferral is pinned to."
  },
  "electron": {
    "deferredLatest": "44.0.0",
    "reviewed": "2026-08-29",
    "reason": "Electron 44 is a major. This repo requires the release notes, npm run check, and a real Windows run of the packaged app before a major lands, and the packaged-app half cannot be judged from CI: the avatar, VRM/VRMA playback and the tray/DPI behaviour only show up on a real desktop. npm run check passes on 43.4.1; this is verification debt, not a known incompatibility. Pair it with the same desktop verification round that three.js is waiting on."
  }
}
